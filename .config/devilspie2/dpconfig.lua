if (get_application_name() == "conky") then
    set_opacity(0.85)
end

if (get_application_name() == "tint2") then
    set_opacity(0.85)
end

if (get_application_name() == "LXTerminal") then
    set_opacity(0.85)
    set_window_size(100, 25)
end

if (get_application_name() == "urxvt") then
    set_opacity(0.85)
    set_window_size(100, 25)
end